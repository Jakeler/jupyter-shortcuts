<html>
    <head>
        <title>Jupyter Lab Keyboard Shortcuts</title>
        <link>
    <link rel="stylesheet" href="style/index.css">
    </head>
    <body>
        {{#shortcuts}}
            <h1 class="section-title">{{title}}</h1>
            {{#options}}
                <div class="shortcut">
                    <div class="meta">
                        <span class="title">{{action}}</span>
                        <span class="id">bla:cat:x</span>
                    </div>
                    <div class="keys-container">
                        {{#keys}}
                            {{#.}}
                                <span class="key">{{.}}</span>
                            {{/.}}
                        {{/keys}}
                    </div>
                </div>
            {{/options}}
        {{/shortcuts}}
    </body>
</html>